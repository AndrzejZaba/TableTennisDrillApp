<Window x:Class="TableTennisDrillApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:TableTennisDrillApp.Views"
        xmlns:local="clr-namespace:TableTennisDrillApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <views:DrillsListMenuView Grid.Column="0" DataContext="{Binding CurrentViewModel}"/>

        <Border Grid.Column="1" Background="#21130d"/>

    </Grid>
</Window>
